public class App {
    public static void main(String[] args) {
        // operadores

        // aritmeticos

        // +,-, /, *, %,
        int num1 = 1;
        int num2 = 2;
        int resultado = num1 + num2;
        resultado = num1 + num2;
        int resultado2 = num1 * num2;
        double resultado3 = num1 / num2;

        // logicos, ralación, comparacion, booleanos

        /*
         * >
         * <
         * >=
         * <=
         * == se usa si un dato es igual a otro
         * != se usa si un dato es diferente a otro
         * && si se cumple una condición y otra condición (and)
         * || si se cumple una condición u otra condición (or)
         * ! negacion ejemplo !true=falso
         */

        // asignacion

        /*
         * =
         * +=
         * =
         * -=
         * /=
         * %=
         */

        // incremento
        // ++

        // decremento
        // --

        // concatenación
        // +
    }
}
